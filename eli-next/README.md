# Éli — Plateforme (Next.js · Supabase · Zero Trust)
Implémentation des Sprints 0→3 du Master Architecture Document. Bi-programme (national/aefe).
`cp .env.example .env.local` puis `npm install && npm run dev`. CI : verrou anti-mock + typecheck + build.
